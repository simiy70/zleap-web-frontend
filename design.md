# 信息源模块设计规范

> 版本：v1.0
>
> 更新日期：2026-07-31
>
> 适用范围：Zleap Web 信息源模块
>
> 现状依据：`src/App.jsx`、`src/styles.css`、`src/components/shell.jsx` 与当前原型资源

## 1. 文档目标

本文档沉淀信息源模块当前已经实现的视觉语言、信息架构与交互规则，作为后续设计迭代、前端开发和验收的统一依据。

本文描述的是当前原型中的设计基线，不包含尚未在界面中体现的产品规划。原型中的模拟数据、固定日期和本地状态仅用于演示交互，不代表最终接口规则。

## 2. 设计定位

信息源模块是 Zleap 中统一管理企业和个人信息资产的工作台。用户在这里完成四类核心任务：

1. 找到信息：搜索公共或个人信息，查看按时间组织的事件。
2. 管理信息：创建信息源容器，上传、记录或同步内容。
3. 判断状态：识别同步进度、失败原因、权限范围和内容归属。
4. 消费内容：进入文档、音频、会话、代码或公共信息源详情，继续阅读和分析。

设计关键词：

- 清晰分层：信息源是容器，信息项是容器内的实际内容。
- 弱化边界：以浅色背景、轻边框和留白为主，降低管理后台的压迫感。
- 状态可见：同步、权限、归属和异常在列表与详情中保持一致。
- 就地操作：高频操作靠近对象出现，低频操作收纳进更多菜单。
- 渐进披露：先展示概况，需要时再展开子项、筛选条件、配置项或详情。

## 3. 核心对象与层级

### 3.1 信息源容器

信息源容器用于组织内容并统一管理权限。界面中以文件夹形态呈现，包含：

- 名称
- 创建人
- 权限范围
- 内容数量
- 创建时间与最近更新时间
- 容器内的信息项

容器权限向下继承。信息项不能脱离所属信息源单独修改权限。

### 3.2 信息项

信息项是用户实际阅读和使用的内容，包括：

- 笔记
- 文档
- 音频
- 网页与 RSS 内容
- 搜索结果
- API 数据
- 代码平台内容
- 知识库内容
- 群聊、机器人和客服会话

信息项可展示来源类型、创建人、同步状态、所属信息源、最近同步时间和创建时间。

### 3.3 公共信息源

公共信息源是面向企业成员的可发现内容集合，使用独立的卡片和详情结构，强调来源介绍、标签、更新时间和站内检索，不与“我的信息源”文件夹层级混用。

### 3.4 汇报信息源

汇报信息源以组织树、成员和时间线为主要结构，用于查看部门或成员的汇报记录。它属于信息源体系中的专门场景，视觉上沿用通用导航、搜索和橙色选中态。

## 4. 信息架构

模块二级导航固定包含：

| 导航 | 默认视图 | 主要任务 |
| --- | --- | --- |
| 信息源搜索 | 时间线 | 在公共或个人范围中搜索事件与原文 |
| 我的信息源 | 卡片 | 管理自己创建的信息源及其内容 |
| 与我共享 | 卡片 | 查看其他成员共享的信息源 |
| 汇报信息源 | 列表 | 按组织、成员和时间查看汇报 |
| 公共信息源 | 卡片 | 浏览企业公共内容集合 |

公共信息源前设置竖向分隔线，使“个人/协作管理”与“公共发现”形成轻量分组。

页面层级：

```text
信息源
├── 信息源搜索
│   ├── 搜索输入
│   ├── 搜索范围
│   ├── 每日事件时间线
│   └── 事件详情
├── 我的信息源 / 与我共享
│   ├── 信息源列表
│   ├── 信息源内页
│   │   ├── 信息项列表
│   │   └── 信息项详情
│   └── 新建、筛选、权限与对象操作
├── 汇报信息源
│   ├── 组织树 / 信息源列表
│   ├── 成员列表
│   ├── 汇报内容
│   └── 汇报时间线
└── 公共信息源
    ├── 标签筛选
    ├── 卡片 / 列表
    └── 公共信息源详情
```

## 5. 页面框架

### 5.1 全局结构

信息源模块使用三段式页面框架：

1. 顶部全局栏：磨玻璃 Header，展示品牌与当前用户。
2. 二级导航栏：承载模块 Tab、搜索、视图切换、筛选和新建。
3. 主内容区：承载卡片、列表、时间线或详情。

底部使用全局悬浮 Dock，当前“信息源”入口使用紫色语义图标与选中状态。

### 5.2 二级导航栏

- 背景：半透明白色磨玻璃，底部轻边框。
- 水平内边距：32px。
- 垂直内边距：10px。
- Tab 高度：40px。
- Tab 字号：14px。
- 默认文字：中性灰。
- 选中文字与图标：橙色。
- 选中指示器：Tab 下方 2px 橙色横线。

右侧工具区按顺序放置：

1. 搜索框
2. 列表/卡片视图切换
3. 筛选按钮，仅“我的信息源”和“与我共享”显示
4. “新建”主按钮，仅在用户有创建权限的场景显示

搜索框默认宽 208px，聚焦后扩展到 256px；有输入时显示清除按钮。

## 6. 视觉规范

### 6.1 色彩

| 语义 | 建议值 | 用途 |
| --- | --- | --- |
| 品牌主色 | `#F97316` / Orange 500 | 主按钮、选中 Tab、重点链接、选中筛选 |
| 品牌浅色 | Orange 50–100 | Hover、选中背景、图标底色 |
| 页面背景 | `#F6F7FB` 至 `#F2F4F9` | 全局浅色渐变背景 |
| 卡片背景 | `#FFFFFF` | 卡片、列表、弹层 |
| 主文字 | Neutral 900 | 标题、对象名称 |
| 次文字 | Neutral 500–600 | 描述、正文、次级信息 |
| 辅助文字 | Neutral 300–400 | 时间、占位符、弱提示 |
| 成功 | Emerald 500–700 | 同步完成、所有人可见 |
| 进行中 | Sky 500 / Amber 500 | 同步中、解析中 |
| 失败 | Red 500 | 同步失败、删除与错误 |
| 部分可见 | Blue 500–700 | 指定人员或部门可见 |

橙色用于“当前选择和主要动作”，不用于大面积铺底。状态色只表达状态，不替代品牌主色。

### 6.2 字体

字体栈：

```css
-apple-system, BlinkMacSystemFont, "PingFang SC", "Segoe UI", sans-serif
```

主要字号层级：

| 场景 | 字号 | 字重 |
| --- | --- | --- |
| 搜索页主标题 | 30px | Bold |
| 页面/弹层标题 | 17–20px | Semibold / Bold |
| 卡片标题 | 14–15px | Semibold / Bold |
| 正文 | 13–14px | Regular |
| 列表正文 | 12–14px | Regular / Medium |
| 标签与辅助信息 | 10–12px | Regular / Medium |

### 6.3 圆角与边界

- 模块基础圆角令牌：16px。
- 信息卡片：16px。
- 主弹层：24px。
- 输入框与工具控件：8–12px。
- 标签与权限徽章：4–999px，按内容语义选择。
- 普通边框：Neutral 100–200，透明度 70%–80%。
- 卡片默认阴影很轻；Hover 时提升一级，不使用重阴影作为常态。

### 6.4 磨玻璃

顶部栏、筛选浮层和新建弹层使用磨玻璃材质：

- 常规玻璃：白色约 58% 不透明度，20px 模糊。
- 强玻璃：白色约 78% 不透明度，24px 模糊。
- 弱玻璃：白色约 40% 不透明度，14px 模糊。

磨玻璃只用于浮层和全局框架，内容卡片保持白色实体表面，保证可读性。

### 6.5 动效

- 通用 Hover/颜色变化：约 150–200ms。
- 卡片 Hover：上移 2px 或文件夹上移 4px，并轻微增强阴影。
- 弹层进入：透明度渐入，同时从 97% 缩放到 100%。
- 同步中图标：1 秒线性循环旋转。
- 录音中状态点：约 1.1 秒呼吸闪烁。
- 空状态插画：3 秒上下浮动，光晕同步脉冲。

动效用于反馈层级和状态，不阻断用户操作。

## 7. 我的信息源与共享信息源

### 7.1 卡片视图

默认使用六列桌面网格：

```css
grid-cols-2 sm:grid-cols-3 lg:grid-cols-6
gap-x: 24px
gap-y: 40px
```

每页最多展示 12 个对象，内容超出后显示统一分页器。

#### 信息源容器卡片

- 最小高度：168px。
- 使用文件夹插画，不增加实体卡片边框。
- 自己创建的容器使用橙色文件夹。
- 自己创建且非全员可见时使用带锁橙色文件夹。
- 他人共享的容器使用蓝色文件夹。
- 内容数量放在文件夹下方，可作为权限入口。
- 名称最多两行，居中显示。
- Hover 时出现右上角更多操作。

新建卡片使用 2px 虚线边框、加号和“新建信息源”文案。Hover 后边框、文字和浅背景切换为橙色。

#### 信息项卡片

- 卡片主体保持正方形。
- 顶部外侧显示“公开 / 部分公开 / 私密”。
- 中央优先展示预览图；没有预览时展示来源类型图标。
- 图标底色随权限变化：公开为天蓝、部分公开为紫色、私密为橙色。
- 名称最多两行。
- Hover 时卡片轻微上移、增强边框与阴影，并显示更多操作。

### 7.2 列表视图

列表采用 12 栅格，列定义如下：

| 列 | 栅格 | 内容 |
| --- | ---: | --- |
| 名称 | 3 | 展开箭头、头像/图标、名称、跨文件夹归属 |
| 权限 | 1 | 权限文字或继承徽章 |
| 类型 | 1 | 信息源、文档、音频等标签 |
| 创建人 | 2 | 头像与姓名 |
| 最近同步 | 2 | 时间，可排序 |
| 创建时间 | 1 | 时间，可排序 |
| 概况/状态 | 2 | 内容数量、同步状态、更多操作 |

交互规则：

- 点击信息源行进入容器内页。
- 点击名称前展开箭头，在当前列表中展开或收起子项。
- 点击信息项行进入详情。
- Hover 行时显示更多操作。
- 名称、最近同步、创建时间支持排序；重复点击切换升降序。
- 展开后的子项使用浅灰底和橙色左边界区分层级。
- 跨文件夹搜索结果在名称下方展示所属信息源，可点击返回该容器。

### 7.3 卡片与列表切换

- 切换控件使用双段式图标按钮。
- 当前视图为白色底、橙色图标和轻阴影。
- 未选中项为灰色，Hover 时加深。
- 视图切换不清除搜索和筛选条件。
- 卡片视图按最近时间排序并分页；列表视图支持列排序。

### 7.4 搜索与跨文件夹结果

“我的信息源”和“与我共享”共用头部搜索。

- 输入关键词后即时匹配名称或来源类型。
- 首页搜索会穿透信息源容器，平铺展示匹配的信息源和信息项。
- 类型或同步状态筛选同样会穿透容器。
- 搜索结果中的信息项必须显示所属信息源，避免丢失上下文。
- 清空搜索后恢复原层级视图。
- 进入信息源内页后，搜索只作用于当前信息源。

### 7.5 筛选

筛选入口为 32px 图标按钮。

- 无条件时为中性灰底。
- 打开或有生效条件时切换为浅橙底、橙色文字与边框。
- 有条件时右上角显示条件数量徽标。
- 浮层宽度：320px。
- 支持类型、权限、同步状态多选。
- 单项选择时展示选中值，多项选择时展示“已选 N 项”。
- 每组可单独清除，底部支持清空全部。
- 点击浮层外关闭，但保留已选择条件。

### 7.6 面包屑与空状态

进入信息源内页后，在内容区顶部展示：

```text
我的信息源 / 当前信息源名称
```

返回上一级时同时清除当前搜索和筛选。

空信息源使用居中空状态：

- 96px 渐变上传图标。
- 标题：“这里还没有内容”。
- 所有者看到“新建信息源”操作链接。
- 非所有者仅看到创建人和暂无内容提示，不展示创建入口。

## 8. 新建与导入

### 8.1 统一新建入口

“新建”按钮打开宽 560px 的强磨玻璃弹层。弹层包含两个 Tab：

1. 上传文件
2. 连接器

弹层点击遮罩或关闭按钮退出。

### 8.2 上传文件

上传区使用三列选项卡片，提供：

- 文档上传
- 音频上传
- 实时录音
- 笔记
- 语雀知识库
- 思源笔记

每个选项由 36px 图标底座、14px 名称和 11px 描述组成。

浏览器插件单独使用横向虚线卡片，明确这是跳转外部安装页的动作。

选择行为：

- 在信息源内创建笔记：直接进入笔记编辑。
- 在首页创建笔记：先选择目标信息源。
- 选择上传类型：进入对应导入流程。
- 选择实时录音：进入悬浮录音流程。

### 8.3 连接器

连接器按来源分组：

- 网络抓取
- 知识库
- 代码平台
- 消息平台
- 开发接口

OAuth 类型显示独立徽章。选择连接器后进入配置流程；需要分步配置的连接器遵循：

1. 信息配置：目标信息源与数据接入字段。
2. 同步设置：信息名称、同步计划与高级配置。

表单主按钮只有在必填条件满足时启用。

### 8.4 实时录音

录音悬浮窗有三种状态：

| 状态 | 颜色 | 可用操作 |
| --- | --- | --- |
| 正在录音 | 红色状态点 | 暂停、结束、最小化 |
| 已暂停 | 琥珀色状态点 | 继续、结束、最小化 |
| 录音完成 | 绿色状态点 | 命名、保存、放弃、最小化 |

最小化后使用右下角胶囊形浮窗，保留计时和核心控制。保存后生成“解析中”的音频信息项。

## 9. 权限设计

### 9.1 权限等级

| 权限 | 颜色 | 说明 |
| --- | --- | --- |
| 私密 | 中性灰 | 仅自己及管理员可见 |
| 部分可见 | 蓝色 | 指定人员或部门可见 |
| 所有人可见 | 绿色 | 企业内所有人可见 |

### 9.2 权限继承

- 权限在信息源容器层设置。
- 容器内所有信息项继承容器权限。
- 信息项在内页展示只读权限徽章，并提示“权限由所属信息源统一管理，无法单独修改”。
- 移动或复制信息项时，必须提前提示目标信息源的权限以及继承结果。
- 修改已有内容的信息源权限时，提示受影响的文件数量。

### 9.3 权限设置弹层

权限弹层最大宽度约 448px，包含：

1. 信息源名称
2. 继承影响提示
3. 三个权限选项卡
4. “部分可见”下的人员/部门选择器
5. 取消与确认

当前权限未变化时，确认按钮禁用。

组织选择器支持：

- 部门树展开/收起
- 人员和部门多选
- 姓名或角色搜索
- 搜索命中高亮
- 已选项 Chip 展示与逐项移除
- 一键清空

## 10. 同步状态与异常反馈

### 10.1 状态语义

| 状态 | 表现 | 说明 |
| --- | --- | --- |
| 同步完成 | 绿色圆形对勾 | 数据可正常使用 |
| 同步中 | 蓝色状态 / 旋转图标 | 正在获取数据 |
| 解析中 | 琥珀色状态 / 旋转图标 | 已获取，正在处理 |
| 同步失败 | 红色圆形叉号 | 需要重试或修复配置 |

状态文案、颜色和图标必须成组出现，不能只依赖颜色表达。

### 10.2 失败重试

- 列表中 Hover“同步失败”后切换为“点击重试”。
- 点击后对象立即进入“解析中”，提供即时反馈。
- 连接器更多菜单提供“立即同步”。
- 正在同步或解析时，“立即同步”禁用并说明原因。
- 需要凭据的连接器失败时，详情中提供“编辑配置”或“重新授权”。
- 失败提示应包含可理解的原因，不只显示错误码。

## 11. 对象操作

更多菜单根据对象类型动态显示：

| 操作 | 信息源容器 | 普通信息项 | 连接器信息项 |
| --- | :---: | :---: | :---: |
| 编辑配置 | — | — | ✓ |
| 立即同步 | — | — | ✓ |
| 重命名 | ✓ | ✓ | ✓ |
| 下载 | — | 文档/音频 | 视类型而定 |
| 权限设置 | ✓ | — | — |
| 复制至 | — | ✓ | ✓ |
| 删除 | ✓ | ✓ | ✓ |

默认信息源不可删除。删除操作使用红色危险样式并要求二次确认；删除信息源时明确说明将同时删除其包含的全部文件且无法撤销。

上下文菜单根据触发按钮的位置自动避让视口边缘，不允许被右侧或底部裁切。

## 12. 信息源搜索

搜索页使用独立的聚焦布局：

- 内容最大宽度：860px。
- 主标题居中。
- 搜索框为 2 层结构：上层输入问题，下层选择搜索范围并提交。
- 搜索框使用橙色边框和轻橙色阴影。
- Enter 和右侧圆形箭头均可提交。
- 搜索范围支持“公共信息”和“个人信息”。

结果以“每日事件”时间线展示：

- 左侧为时间，右侧为事件卡片。
- 时间轴使用 1px 中性灰竖线。
- 事件卡片展示标题、两行摘要、来源和相关子事项数量。
- 点击卡片打开事件详情。
- 点击“相关子事项”在当前卡片内展开或收起。

事件详情包含：

- 事件标题
- 属性标签
- 摘要
- 原文参考与链接
- 原文标题和正文

## 13. 公共信息源

公共信息源顶部使用横向标签 Chip：

- 选中：橙色实底、白色文字。
- 未选中：白底灰字、浅灰边框。
- 标签区域可横向滚动。

卡片视图：

- 响应式 1 / 2 / 3 / 4 列。
- 卡片最小高度 168px。
- 展示来源头像、名称、两行描述和标签。
- Hover 时上移、边框变橙、名称变橙、右箭头前移。

列表视图：

- 列为名称、描述、标签、最近更新。
- 行 Hover 使用浅橙背景。
- 点击卡片或行进入公共信息源详情。

无结果时展示搜索图标、主提示和“换个关键词或标签试试”的辅助文案。

## 14. 汇报信息源

汇报信息源默认使用列表视图，支持搜索组织、成员或信息源。

主要交互：

- 组织树支持展开和收起。
- 点击叶节点或带信息源的部门进入汇报详情。
- 详情左侧展示近期成员，右侧展示选中成员的汇报内容。
- 当前成员使用浅橙背景和橙色左边界。
- 汇报内容沿用会话气泡结构，超过 5 分钟或跨日期时增加时间分隔。
- “全部成员”打开覆盖主内容区的成员选择页。
- 时间线支持关键词搜索、日期筛选、事件详情和查看原文。

## 15. 弹层与反馈

### 15.1 通用弹层

- 遮罩：黑色 30%–40%，可带轻微背景模糊。
- 常规宽度：384–560px。
- 最大高度：约 82vh–86vh，内容区独立滚动。
- Header 和 Footer 固定，中间内容滚动。
- 右上角统一使用关闭按钮。
- 点击遮罩可关闭非强制流程。

### 15.2 按钮优先级

- 主按钮：橙色实底，用于确认、创建、保存。
- 次按钮：白底灰边，用于取消或返回。
- 文字按钮：用于轻量跳转、清空和辅助操作。
- 危险按钮：红色实底，仅用于不可逆操作。
- 禁用按钮：中性灰底、低对比文字、禁用光标。

### 15.3 输入反馈

- 默认边框为 Neutral 200。
- 聚焦时使用 Orange 300 边框与 Orange 100 外环。
- 错误场景使用红色提示与可行动文案。
- 搜索无结果时保留已输入关键词，提供清晰空状态。

## 16. 响应式与可用性

当前界面以桌面端为主，设计基线建议宽度不低于 1024px。

- 卡片网格在小尺寸下从 6 列依次降为 3 列和 2 列。
- 公共信息源卡片从 4 列依次降为 3、2、1 列。
- 弹层宽度使用 `max-width: calc(100vw - 32px)`，避免贴边。
- 二级导航内容较多时，应保证 Tab 不压缩；右侧工具区保持可操作。
- 长名称使用截断或两行省略，并通过 `title` 提供完整内容。
- 图标按钮必须有 `title` 或 `aria-label`。
- 可点击区域建议不小于 32px；核心按钮建议不小于 36px。
- 选中、成功、失败不能仅用颜色区分，应同时使用文字、图标或边框。
- 列表行和卡片应支持键盘聚焦；正式产品需补充 Enter/Space 激活与可见焦点样式。

## 17. 关键状态保留规则

| 操作 | 应保留 | 应重置 |
| --- | --- | --- |
| 卡片/列表切换 | 搜索、筛选、当前导航 | 当前卡片分页可回到第一页 |
| 切换二级导航 | 对应页面自己的视图偏好 | 当前文件夹、搜索、筛选 |
| 进入信息源内页 | 当前导航、视图模式 | 首页搜索结果 |
| 返回信息源首页 | 视图模式 | 当前文件夹、搜索、筛选、选中对象 |
| 清空筛选 | 搜索词 | 类型、权限、状态条件 |
| 新建完成 | 当前信息源上下文 | 新建弹层内部临时状态 |
| 重试同步 | 当前列表位置 | 失败状态切换为解析中 |

## 18. 组件复用建议

后续拆分 `src/App.jsx` 时，建议保留以下组件边界：

```text
InfoSourcePage
├── SourceSubNav
├── SourceToolbar
│   ├── CompactSearch
│   ├── ViewToggle
│   └── HeaderFilterButton
├── CardView
│   ├── SourceFolderCard
│   └── SourceItemCard
├── ListView
│   ├── SourceTableHeader
│   └── SourceTableRow
├── ScopeBadge
├── StatusBadge / StatusIcon
├── SourceCreateDialog
├── PermissionPopover
├── ContextMenu
├── SourceSearchPage
├── ReportPage
└── PublicSourcesPage
```

共享令牌应继续从 `src/styles.css` 与 Tailwind Theme 中读取，避免在各页面重复定义橙色、圆角、阴影和状态色。

## 19. 设计验收清单

### 导航与布局

- [ ] 五个二级导航入口完整，选中态与内容一致。
- [ ] 搜索、视图、筛选和新建按权限正确显示。
- [ ] 信息源内页有可返回的面包屑。
- [ ] 全局 Header 与底部 Dock 不遮挡内容。

### 卡片与列表

- [ ] 信息源容器与信息项视觉可明显区分。
- [ ] 自有、共享和受限文件夹使用正确资源。
- [ ] 卡片每页最多 12 项并正确分页。
- [ ] 列表列宽、排序、展开和 Hover 操作符合规范。
- [ ] 跨文件夹结果展示所属信息源。

### 搜索与筛选

- [ ] 搜索可匹配名称和来源类型。
- [ ] 首页搜索和类型/状态筛选可穿透容器。
- [ ] 筛选数量徽标与实际条件数量一致。
- [ ] 清空搜索和筛选后恢复原始层级。

### 权限

- [ ] 三档权限的文案、颜色和说明一致。
- [ ] 信息项明确展示继承关系。
- [ ] 修改容器权限前提示影响范围。
- [ ] 复制或移动前提示目标权限。

### 状态与异常

- [ ] 四种同步状态均有图标、颜色和文案。
- [ ] 失败状态提供重试或修复配置入口。
- [ ] 重试后即时进入处理中状态。
- [ ] 删除操作有二次确认和不可逆提示。

### 弹层与可访问性

- [ ] 弹层不超出视口，长内容可滚动。
- [ ] 遮罩、关闭、取消和确认行为一致。
- [ ] 图标按钮有可读标签。
- [ ] 禁用状态不可触发。
- [ ] 键盘焦点清晰，核心流程可用键盘完成。
