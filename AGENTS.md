# Zleap Web 前端协作规则

- 本仓库是 Zleap Web 前端原型的规范 checkout；不要从 Desktop、New project 或上层目录启动开发任务。
- 开始任务先运行 `git status --short --branch`，确认分支和用户已有修改；并行开发优先使用 Git worktree。
- 权威实现位于 `src/`，产品背景与文档入口见 `docs/product-context.md`。
- 新产品决策先更新对应 `docs/` 文档，再同步代码；不要把截图或导出物当作业务规则来源。
- 设计验收截图放在 `docs/design-qa/`，临时上传与测试产物放在既有 QA 目录或任务工作目录。
- 实现完成后运行 `npm run verify`；交互或视觉改动还需使用 Browser 验证受影响页面、响应式布局和控制台错误。
- 只提交本次任务产生的文件，不得混入任务开始前已有修改。
- 实现性任务验证通过后提交并推送当前分支；只读审查、无文件改动或无远端时不创建空提交。
- 删除、覆盖、重写历史或部署前必须确认目标与影响范围。
